import React from 'react'

interface Props {
    title: string;
    imageSrc: string;
    id: string;
    currentUser?: string;
}

const PostHead = ({title, imageSrc, id, currentUser}:Props) => {
  return (
    <>
        
    </>
  )
}

export default PostHead